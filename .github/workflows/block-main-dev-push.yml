name: Block direct pushes to protected branches

on:
  push:
    branches:
      - main
      - dev

jobs:
  block-direct-push:
    runs-on: ubuntu-latest
    steps:
      - name: Fail if push is direct to main or dev
        run: |
          echo "Direct pushes to main or dev are not allowed. Use a pull request."
          exit 1
