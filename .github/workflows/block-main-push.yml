name: Block direct pushes to main

on:
  push:
    branches:
      - main

jobs:
  block-direct-push:
    runs-on: ubuntu-latest
    steps:
      - name: Fail if push is direct to main
        run: |
          echo "Direct pushes to main are not allowed. Use a pull request."
          exit 1
